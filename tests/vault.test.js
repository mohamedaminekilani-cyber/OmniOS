import {test}from'node:test';import assert from'node:assert/strict';import'../js/crypto-vault.js';const C=globalThis.SecondBrainCrypto;
test('credential ciphertext round-trips and rejects wrong key',async()=>{const salt=crypto.getRandomValues(new Uint8Array(16)),pass='long synthetic test passphrase',key=await C.key(pass,salt),box=await C.seal([{password:'SYNTHETIC_SECRET'}],key,salt);assert.equal(JSON.stringify(box).includes('SYNTHETIC_SECRET'),false);assert.equal((await C.open(box,pass)).value[0].password,'SYNTHETIC_SECRET');await assert.rejects(C.open(box,'wrong test passphrase'))});
test('tampering and weak passphrases rejected',async()=>{await assert.rejects(C.key('short',new Uint8Array(16)));await assert.rejects(C.open({version:1,iterations:1,cipher:''},'a long enough passphrase'))});
